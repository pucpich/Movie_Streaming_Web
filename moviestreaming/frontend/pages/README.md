# Pages

Chứa các file trang web (vd: `index.html`, `movie.html`, `favorites.html`) để tách rõ khỏi thư mục asset.

Hiện tại các trang vẫn nằm ở thư mục `frontend/` để tránh làm gãy link. Bạn có thể chuyển vào đây khi muốn.
