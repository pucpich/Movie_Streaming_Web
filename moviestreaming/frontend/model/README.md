# Model / Logic

Nơi đặt các file JavaScript kiểu helper/model (vd: `api.js`, `auth.js`, `movie.js`) để tránh nhét logic inline trong HTML.

Hiện dự án đang dùng script inline, nên thư mục này tạo sẵn để bạn dễ mở rộng sau.
